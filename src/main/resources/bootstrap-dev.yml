spring:
  cloud:
    config:
      label: master
      profile: dev
      name: oauth2-server
      discovery:
        service-id: config-server
        enabled: true
      username: admin
      password: admin